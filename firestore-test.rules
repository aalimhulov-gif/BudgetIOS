// ВРЕМЕННЫЕ ПРАВИЛА ДЛЯ ТЕСТИРОВАНИЯ (НЕБЕЗОПАСНО!)
// Используйте ТОЛЬКО для проверки что приложение работает
// После тестирования ОБЯЗАТЕЛЬНО замените на правила из firestore.rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

/*
ЭТИ ПРАВИЛА:
- Разрешают читать и писать ВСЕМ авторизованным пользователям
- НЕ БЕЗОПАСНЫ для продакшена!
- Используйте ТОЛЬКО для проверки что приложение работает
- После успешного тестирования замените на правила из firestore.rules
*/